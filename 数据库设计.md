## 数据库设计

#### 1. 用户信息表 (`users`)

*系统需要管理求职者和管理员。*

- `user_id` (PK): 用户唯一标识
- `role`: 角色 (如：candidate 候选人, admin 管理员)
- `username`: 用户名/手机号
- `password_hash`: 加密密码
- `create_time`: 注册时间

#### 2. 岗位信息表 (`jd_information`)

*增加结构化字段，方便 AI 提取重点进行提问。*

- `jd_id` (PK): 岗位 ID
- `title`: 岗位名称 (例如：Java高级工程师)
- `jd_url`: 原始链接 (可选)
- `jd_content`: 岗位详细描述文本
- `company_name`: 企业名称
- `salary_range`: 薪资范围
- `work_experience`: 要求工作经验
- `core_skills`: 核心技能要求 (建议存 JSON 格式，如 `["Java", "Spring Boot", "MySQL"]`，方便喂给大模型)
- `education`: 学历要求
- `create_time`: 创建时间

#### 3. 简历信息表 (`resume_information`)

*增加结构化解析字段，作为 AI 提问的背景上下文。*

- `resume_id` (PK): 简历 ID
- `user_id` (FK): 候选人 ID
- `file_url`: 简历源文件存储路径 (PDF/Word)
- `parsed_content`: 简历结构化内容 (存 JSON，包含教育经历、工作经历等，由 AI 或简历解析工具生成)
- `create_time`: 上传时间

#### 4. 面试记录表 (`interview_record`) 

*控制整场面试的生命周期。*

- `interview_id` (PK): 面试记录 ID
- `user_id` (FK): 候选人 ID
- `jd_id` (FK): 关联的岗位 ID
- `resume_id` (FK): 关联的简历 ID
- `status`: 面试状态 (如：0-待开始, 1-进行中, 2-已完成, 3-已出报告)
- `start_time`: 面试开始时间
- `end_time`: 面试结束时间

#### 5. 面试对话流水表 (`interview_dialogue`)

*由于是大模型驱动，必须有一张表专门记录一来一回的对话上下文，用于后续追问和最终评分。*

- `dialogue_id` (PK): 对话记录 ID
- `interview_id` (FK): 关联的面试记录 ID
- `role`: 说话角色 (`ai_interviewer` 或 `candidate`)
- `content`: 具体的对话文本内容
- `audio_url`: 如果有语音面试，存储对应的录音切片地址
- `tokens_used`: 本次对话消耗的 Token 数量
- `create_time`: 发言时间

#### 6. 面试评估报告表 (`interview_evaluation`) —— **(AI 系统的核心新增)**

*面试结束后，AI 根据 `interview_dialogue` 生成的打分和评价。*

- `evaluation_id` (PK): 评估 ID
- `interview_id` (FK): 关联的面试记录 ID (唯一约束)
- `total_score`: 综合总分
- `dimension_scores`: 各维度得分 (存 JSON，如 `{"沟通表达": 85, "技术深度": 90, "抗压能力": 75}`)
- `ai_summary`: AI 生成的综合评价总结
- `improvement_advice`: AI 给出的改进建议
- `create_time`: 报告生成时间