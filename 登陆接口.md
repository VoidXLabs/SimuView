# SimuView 后端接口文档

## 认证模块

### 1. 用户登录

#### 接口信息
| 属性 | 值 |
|------|-----|
| **URL** | `/api/v1/auth/login` |
| **方法** | `POST` |
| **Content-Type** | `application/json` |
| **是否需要认证** | 否 |

#### 请求参数

| 字段名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| `email` | `string` | 是 | 用户邮箱 |
| `password` | `string` | 是 | 用户密码 |

#### 请求示例

```json
POST /api/v1/auth/login
Content-Type: application/json

{
  "email": "user@example.com",
  "password": "password123"
}
```

#### 成功响应 (200 OK)

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `success` | `boolean` | 操作是否成功 |
| `message` | `string` | 提示信息 |
| `data` | `object` | 用户信息 |
| `data.id` | `string` | 用户唯一标识 |
| `data.email` | `string` | 用户邮箱 |
| `data.name` | `string` | 用户姓名 |
| `data.avatar` | `string` (可选) | 用户头像URL |

**响应示例：**

```json
{
  "success": true,
  "message": "Login successful",
  "data": {
    "id": "123e4567-e89b-12d3-a456-426614174000",
    "email": "user@example.com",
    "name": "John Doe",
    "avatar": "https://example.com/avatar.jpg"
  }
}
```

#### 失败响应

| HTTP状态码 | 说明 |
|------------|------|
| `400 Bad Request` | 请求参数错误（邮箱或密码为空） |
| `401 Unauthorized` | 邮箱或密码错误 |
| `403 Forbidden` | 账户已被禁用 |

**401 响应示例：**

```json
{
  "success": false,
  "message": "Invalid email or password",
  "data": null
}
```

#### Cookie 设置

登录成功后，后端会通过 `Set-Cookie` 响应头设置会话 Cookie：

```http
Set-Cookie: session=abc123def456; Path=/; HttpOnly; Secure; SameSite=Strict
```

| Cookie名称 | 说明 |
|------------|------|
| `session` | 会话标识，用于后续请求的身份验证 |

---

### 2. 用户登出

#### 接口信息
| 属性 | 值 |
|------|-----|
| **URL** | `/api/v1/auth/logout` |
| **方法** | `POST` |
| **Content-Type** | `application/json` |
| **是否需要认证** | 是（需携带 Cookie） |

#### 请求示例

```json
POST /api/v1/auth/logout
Cookie: session=abc123def456

{}
```

#### 成功响应 (200 OK)

```json
{
  "success": true,
  "message": "Logout successful",
  "data": null
}
```

#### Cookie 清除

后端会通过 `Set-Cookie` 响应头清除会话 Cookie：

```http
Set-Cookie: session=; Path=/; expires=Thu, 01 Jan 1970 00:00:00 UTC
```

---

## 请求头要求

除登录接口外，其他所有接口请求都需要携带以下请求头：

| 请求头 | 说明 |
|--------|------|
| `X-User-Id` | 用户ID（从登录响应的 `data.id` 获取） |
| `Cookie` | 会话 Cookie（自动携带） |

**示例：**

```http
GET /api/v1/interviews
Host: localhost:8080
X-User-Id: 123e4567-e89b-12d3-a456-426614174000
Cookie: session=abc123def456
```

---

## 错误响应格式

所有错误响应统一使用以下格式：

```json
{
  "success": false,
  "message": "Error description",
  "data": null
}
```