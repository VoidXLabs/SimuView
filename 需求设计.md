# 模拟面试系统需求设计

## 主要功能流程

1. 用户提交boss直聘岗位信息页面的URL和用户的简历
2. 使用爬虫程序提取岗位信息的关键词、岗位内容，使用OCR技术提取用户简历中的信息。
3. 用户选择面试模式：温和/压力
4. 将2中获取的所有文本和面试模式作为大模型的初始提示词

初始提示词示例:

```
# Role
你是一位拥有 10 年 [此处插入岗位名称] 行业经验的资深面试官，目前正在主持一场针对以下岗位的模拟面试。

# Context

    目标岗位需求 (JD): [此处插入爬虫提取的关键词/内容]

    候选人背景 (Resume): [此处插入 OCR 提取的简历信息]
    
    公司名称: [此处插入公司名称]
    
    需要具备的技能: [此处插入技能]

    面试风格: [温和模式 / 压力面试模式]

# Interview Guidelines

    开场白： 请先进行简短的自我介绍，并邀请候选人做开场。

    循序渐进： 每次只提一个问题。严禁一次性抛出多个问题。

    深度挖掘： 结合 JD 要求和简历中的项目细节进行提问。如果候选人回答模糊，请针对该点进行追问（尤其是压力模式下）。

    风格控制：

        温和模式： 语气鼓励，引导候选人展现长处，对回答给予正向反馈。

        压力模式： 语气严肃，针对技术漏洞或逻辑矛盾进行尖锐追问，考察心理素质。

    禁止出戏： 严禁以 AI 助手的身份说话，始终保持面试官的人设。

# Output Format
直接开始对话。
```



### 爬虫程序

输入：岗位信息的URL

输出：

```
{
  "request_id": "req_20240520_001",
  "url": "https://example.com/jobs/1234",
  "source_platform": "BossZhipin",
  "timestamp": "2024-05-20 14:30:00",
  "status": "success",
  "data": {
    "job_title": "Java后端开发工程师",
    "company_name": "某互联网大厂",
    "salary_range": "20k-35k",
    "raw_jd_text": "1. 负责核心业务开发；2. 精通 Spring Cloud...",
    "key_requirements": [
      "Java核心技术栈",
      "微服务架构设计",
      "MySQL性能调优",
      "分布式锁/消息队列"
    ],
    "work_experience": "3-5年",
    "education": "本科及以上"
  }
}
```





### 接口设计

准备面试页面原型图：

![img](./images/面试初始化原型图1.png "")



面试初始化时序图:

![img](./images/面试初始化时序图.png "")



> 当用户点击准备面试按钮时，前端view_id,这是是本次面试的唯一凭证。



### WebSocket

**进入准备面试页面时** 前端立刻使用 `view_id` 发起 WebSocket 连接，并在后端注册这个 `view_id` 的通道。



#### 岗位信息获取接口

```
核心流程设计

    前端：用户输入 URL，点击“解析”。前端校验 URL 格式后，通过 HTTP请求把 `(view_id, url)` 发给后端。

    后端接收到请求，将爬虫任务扔进异步线程或消息队列（如 Celery），并立即返回 HTTP 200 {"msg": "任务已受理"}。

	前端进入 Loading 状态（全屏遮罩或按钮置灰），展示进度条。
	
    后端爬虫在执行到不同阶段时，通过 WebSocket 向对应的 view_id 推送状态（如：解析DOM中 -> 提取关键词中 -> 存入数据库完成）。前端收到状态包，动态更新 UI 上的 Loading 文字。
    
    任务终态： 成功： 前端收到成功指令，解除 Loading，展示解析出来的岗位信息，点亮“开始面试”按钮。
```





#### 解析岗位信息接口

 用户粘贴 URL 和上传简历, 点击“开始解析:

页面展示一个进度条或三个 Loading 状态（JD 抓取中... 简历解析中... 提示词组装中...）。

```
核心流程设计 (Sequence)

    前端：用户选择 PDF/PNG 简历，调用常规 POST 接口先将文件上传至服务器（获取文件 ID 或路径）。

    前端：文件上传成功后，立即建立 WebSocket 连接，发送 {"action": "start_parse", "file_id": "xxx"}。

    后端 (OCR Service)：启动异步任务，调用 OCR 引擎（如 PaddleOCR 或 腾讯云 OCR）。

    状态推送：后端通过 WebSocket 实时推送：“正在读取文件...”、“正在提取工作经历...”、“正在分析技术栈...”。

    数据入库：解析完成后，将结构化 JSON 存入数据库，并向前端发送 success 信号。
```



#### 进入面试接口









数据库设计:
view_information(面试信息表): view_id user_id jd_url  resume_id 
jd_information(岗位信息表): jd_id  jd_url  jd_summary
resume_information(简历信息表)： resume_id 